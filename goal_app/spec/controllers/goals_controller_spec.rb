require 'rails_helper'

RSpec.describe GoalsController, type: :controller do
  # describe "POST #create" do
  #   it "creates a new goal" do
  #     post :create, params: { goal: { body: "lose ten pounds", public: true, completed: true, user_id: user.id} }
  #     expect(Goal.count).to eq(1)
  #   end    
  #   it "assigns the correct :user_id" do
  #     post :create, params: { goal: { body: "lose ten pounds", public: true, completed: true, user_id: user.id} }
  #     goal = Goal.find_by(body: "lose ten pounds")
  #     expect(goal.user_id).to eq(user.id)
  #   end
  # end
end
